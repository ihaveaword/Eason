🎨 更新图标并重新打包的完整步骤

   方法1: 一键更新（推荐）⭐
     # 1. 替换为新图标（重命名）
     cd /Users/code/Eason
     mv Eason.icns Eason-old.icns  # 备份旧图标
     cp Eason-removebg-preview.icns Eason.icns  # 使用新图标

     # 2. 清理旧的构建文件
     rm -rf dist/ build/ *.spec

     # 3. 重新打包应用（会自动使用 Eason.icns）
     ./scripts/build_app.sh

     # 4. 测试应用
     open dist/Eason.app

     # 5. 创建新的 DMG
     ./scripts/create_dmg.sh 1.2.3

     # 6. 查看结果
     ls -lh release/v1.0.2/