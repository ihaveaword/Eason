# 🌐 pip 镜像源配置说明

## 问题说明

如果你遇到：
- ❌ 安装速度慢（超过 5 分钟）
- ❌ 连接超时 `SSLError`
- ❌ 下载失败

这是因为默认的 PyPI 服务器在国外，访问速度慢。

## 解决方案：使用国内镜像源 🚀

### 方法 1: 使用项目自带的配置（推荐）

项目已经配置好了清华镜像源，直接运行：

```bash
./install.sh
```

或者如果遇到问题：

```bash
./fix_install.sh
```

### 方法 2: 手动指定镜像源

如果自动安装失败，手动安装：

```bash
# 激活虚拟环境
source .venv/bin/activate

# 使用清华镜像源
pip install PyQt6 -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或使用阿里云镜像源
pip install PyQt6 -i https://mirrors.aliyun.com/pypi/simple/
```

### 方法 3: 永久配置镜像源

编辑 `~/.pip/pip.conf`（或在项目中已提供 `pip.conf`）：

```ini
[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple
timeout = 60

[install]
trusted-host = pypi.tuna.tsinghua.edu.cn
```

## 🌐 常用国内镜像源

### 1. 清华大学（推荐）⭐️

```bash
pip install PyQt6 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**特点**：
- ✅ 速度快
- ✅ 稳定
- ✅ 更新及时

### 2. 阿里云

```bash
pip install PyQt6 -i https://mirrors.aliyun.com/pypi/simple/
```

**特点**：
- ✅ 覆盖全国
- ✅ 速度稳定

### 3. 腾讯云

```bash
pip install PyQt6 -i https://mirrors.cloud.tencent.com/pypi/simple
```

### 4. 中科大

```bash
pip install PyQt6 -i https://pypi.mirrors.ustc.edu.cn/simple/
```

### 5. 豆瓣

```bash
pip install PyQt6 -i https://pypi.douban.com/simple/
```

## 📊 速度对比（参考）

| 镜像源 | 速度 | 稳定性 | 推荐度 |
|--------|------|--------|--------|
| 清华 | ⭐️⭐️⭐️⭐️⭐️ | ⭐️⭐️⭐️⭐️⭐️ | ⭐️⭐️⭐️⭐️⭐️ |
| 阿里云 | ⭐️⭐️⭐️⭐️⭐️ | ⭐️⭐️⭐️⭐️ | ⭐️⭐️⭐️⭐️ |
| 腾讯云 | ⭐️⭐️⭐️⭐️ | ⭐️⭐️⭐️⭐️ | ⭐️⭐️⭐️⭐️ |
| 官方 PyPI | ⭐️ | ⭐️⭐️⭐️⭐️⭐️ | ⭐️ |

## 🔧 测试镜像源速度

```bash
# 测试清华源
time pip download --no-deps PyQt6 -i https://pypi.tuna.tsinghua.edu.cn/simple

# 测试阿里云源
time pip download --no-deps PyQt6 -i https://mirrors.aliyun.com/pypi/simple/
```

选择耗时最短的镜像源。

## 💡 实用技巧

### 临时使用镜像源

```bash
pip install 包名 -i 镜像源地址
```

### 永久配置（推荐）

**macOS/Linux**:

```bash
mkdir -p ~/.pip
cat > ~/.pip/pip.conf << EOF
[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple
timeout = 60

[install]
trusted-host = pypi.tuna.tsinghua.edu.cn
EOF
```

**Windows**:

在 `C:\Users\用户名\pip\pip.ini` 创建：

```ini
[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple
timeout = 60

[install]
trusted-host = pypi.tuna.tsinghua.edu.cn
```

### 查看当前配置

```bash
pip config list
```

### 取消镜像源（恢复官方）

```bash
pip config unset global.index-url
```

## 🆘 常见问题

### Q1: 为什么配置了镜像源还是慢？

**可能原因**：
1. 网络问题 - 检查网络连接
2. 防火墙/代理 - 检查防火墙设置
3. 镜像源同步延迟 - 换一个镜像源试试

**解决方法**：
```bash
# 尝试不同的镜像源
./fix_install.sh
```

### Q2: 出现 SSL 错误？

**解决方法**：
```bash
pip install PyQt6 -i https://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn
```

### Q3: 如何加速 pip 本身的升级？

```bash
pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q4: 虚拟环境中如何配置？

虚拟环境会继承系统配置，也可以单独配置：

```bash
source .venv/bin/activate
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

## 📦 项目已做的优化

本项目已经做了以下优化：

1. ✅ 提供 `pip.conf` 配置文件
2. ✅ `install.sh` 自动使用清华镜像源
3. ✅ 添加超时设置（避免长时间等待）
4. ✅ 提供 `fix_install.sh` 快速修复工具
5. ✅ 跳过已安装的包（节省时间）

## 🚀 快速开始

如果你是第一次安装，推荐：

```bash
# 方法1：使用自动脚本（会自动选择镜像源）
./install.sh

# 方法2：如果遇到问题，使用修复工具
./fix_install.sh

# 方法3：完全手动
source .venv/bin/activate
pip install PyQt6 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 📚 相关资源

- [清华大学开源镜像站](https://mirrors.tuna.tsinghua.edu.cn/)
- [阿里云开源镜像站](https://developer.aliyun.com/mirror/)
- [pip 官方文档](https://pip.pypa.io/en/stable/)

---

**💡 提示**：如果安装超过 5 分钟，建议停止并使用 `./fix_install.sh` 重新安装。
